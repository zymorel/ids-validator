{
  "name": "ids-validator",
  "version": "1.0.0",
  "description": "IDS Validator — BIM Quality Control (offline, no backend)",
  "type": "module",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "node scripts/copy-wasm.js",
    "electron:dev": "npm run build && electron .",
    "build:exe": "npm run build && electron-builder --config electron-builder.json"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "three": "^0.175.0",
    "web-ifc": "^0.0.74"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "vite": "^6.0.0"
  }
}
